import 'package:flutter/material.dart';

class NameProfessionWidget extends StatelessWidget {
  const NameProfessionWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('Eleonora'),
        Text('UI/UX Designer'),
      ],
    );
  }
}
